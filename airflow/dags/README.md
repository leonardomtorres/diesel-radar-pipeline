# DAGs do Airflow

Aqui vão morar os arquivos Python que definem os pipelines (DAGs) do Airflow: a orquestração da ingestão dos dados da ANP e o disparo do `dbt run` / `dbt test`.

Ainda vazio — chega no Task 7 do projeto.
