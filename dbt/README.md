# Projeto dbt

Aqui vai ficar o projeto dbt com os models das camadas bronze, silver e gold, mais os testes de qualidade de dado.

Ainda vazio — vamos gerar a estrutura oficial do dbt (via `dbt init`) no Task 6.
